{
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ],

  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],

  "env": {
    "API_URL": "https://api.example.com"
  }
}
